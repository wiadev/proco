'use strict';

var utils = require('../utils');

var worker = function worker(data, progress, resolve, reject) {};

var key = 'APP_PUSH_RELEASE';

module.exports = {
    worker: worker,
    key: key
};