import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  profileLoop: {
    position: 'relative',
    flex: 1
  },
  video: {
    position: 'absolute',
    top: 0,
    left: 0
  },
  container: {
    flex: 1
  }
});

export default styles;
