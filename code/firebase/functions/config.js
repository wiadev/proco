// If you need to require something here other than files like this and JSON files, you are doing it wrong.

let config = {};

config.jwtSecret = `A_BIG_FREAKING_SECRET_"9G83E__J![:"]#?;}5&E?xDzr,i':L'`;

module.exports = config;
